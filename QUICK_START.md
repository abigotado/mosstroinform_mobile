# 🚀 Быстрый старт

Краткое руководство для быстрого запуска приложения.

## Для опытных разработчиков

Если у вас уже установлен Flutter:

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/abigotado/mosstroinform_mobile.git
cd mosstroinform_mobile

# 2. Установите зависимости
flutter pub get

# 3. Сгенерируйте код
flutter pub run build_runner build --delete-conflicting-outputs

# 4. Запустите приложение
flutter run
```

Готово! Приложение запустится в режиме разработки с моковыми данными.

## Для новичков (пошаговая инструкция)

### Шаг 1: Установка Flutter

1. Перейдите на [flutter.dev](https://flutter.dev/docs/get-started/install)
2. Скачайте Flutter SDK для вашей операционной системы
3. Распакуйте архив в удобное место (например, `~/development/flutter`)

**macOS/Linux:**
```bash
# Добавьте Flutter в PATH (добавьте в ~/.zshrc или ~/.bashrc)
export PATH="$PATH:$HOME/development/flutter/bin"

# Перезагрузите терминал или выполните:
source ~/.zshrc  # или source ~/.bashrc
```

**Windows:**
- Добавьте путь к Flutter в переменные окружения PATH через Панель управления

### Шаг 2: Проверка установки

```bash
flutter doctor
```

Эта команда покажет, что нужно установить. Обычно требуется:
- ✅ Flutter SDK (уже установлен)
- ⚠️ Android toolchain (для Android разработки)
- ⚠️ Xcode (для iOS разработки, только macOS)
- ⚠️ VS Code или Android Studio (рекомендуется)

### Шаг 3: Установка Android Studio (для Android)

1. Скачайте [Android Studio](https://developer.android.com/studio)
2. Установите Android Studio
3. Запустите Android Studio → More Actions → SDK Manager
4. Установите Android SDK Platform 33 или выше
5. Вернитесь в терминал и выполните: `flutter doctor --android-licenses`
6. Примите все лицензии (нажимайте `y`)

### Шаг 4: Установка VS Code (рекомендуется)

1. Скачайте [VS Code](https://code.visualstudio.com/)
2. Установите VS Code
3. Откройте VS Code → Extensions (⌘+Shift+X)
4. Найдите и установите расширение "Flutter"

### Шаг 5: Клонирование проекта

```bash
git clone https://github.com/abigotado/mosstroinform_mobile.git
cd mosstroinform_mobile
```

### Шаг 6: Установка зависимостей

```bash
flutter pub get
```

Эта команда скачает все необходимые библиотеки.

### Шаг 7: Генерация кода

Проект использует code generation для создания моделей данных и провайдеров:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

Это может занять 1-2 минуты. Дождитесь завершения.

### Шаг 8: Запуск эмулятора/симулятора

**Android:**
1. Откройте Android Studio
2. Tools → Device Manager
3. Нажмите "Create Device"
4. Выберите устройство (например, Pixel 5) → Next
5. Выберите образ системы (например, API 33) → Next → Finish
6. Нажмите ▶️ (Play) рядом с созданным устройством

**iOS (только macOS):**
1. Откройте Xcode
2. Xcode → Open Developer Tool → Simulator
3. File → New Simulator
4. Выберите устройство (например, iPhone 14) → Create
5. Simulator автоматически запустится

### Шаг 9: Запуск приложения

```bash
flutter run
```

Приложение автоматически определит запущенное устройство и установится на него.

**Если устройств несколько:**
```bash
# Посмотреть список устройств
flutter devices

# Запустить на конкретном устройстве
flutter run -d <device_id>
```

### Шаг 10: Готово! 🎉

Приложение должно запуститься и показать список проектов.

## Что делать дальше?

1. **Изучите приложение:**
   - Просмотрите список проектов
   - Откройте детали проекта
   - Отправьте запрос на строительство
   - Согласуйте документы
   - Посмотрите видеопотоки с камер

2. **Изучите код:**
   - Начните с `lib/main.dart` - точка входа
   - Изучите структуру фич в `lib/features/`
   - Посмотрите примеры экранов в `lib/features/*/ui/screens/`

3. **Прочитайте документацию:**
   - [README.md](./README.md) - общая информация
   - [ARCHITECTURE.md](./ARCHITECTURE.md) - архитектура проекта
   - [BUILD_GUIDE.md](./BUILD_GUIDE.md) - сборка приложения

## Частые проблемы и решения

### Проблема: "flutter: command not found"

**Решение:** Flutter не добавлен в PATH. См. Шаг 1.

### Проблема: "No devices found"

**Решение:** 
- Убедитесь, что эмулятор/симулятор запущен
- Для Android: проверьте, что ADB видит устройство: `adb devices`
- Для iOS: убедитесь, что симулятор запущен

### Проблема: "Error: Cannot find module"

**Решение:**
```bash
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### Проблема: "Gradle build failed"

**Решение:**
```bash
cd android
./gradlew clean
cd ..
flutter clean
flutter pub get
```

### Проблема: "CocoaPods not installed" (iOS)

**Решение:**
```bash
sudo gem install cocoapods
cd ios
pod install
cd ..
```

## Полезные команды

```bash
# Проверка состояния Flutter
flutter doctor

# Список доступных устройств
flutter devices

# Очистка проекта
flutter clean

# Установка зависимостей
flutter pub get

# Генерация кода
flutter pub run build_runner build --delete-conflicting-outputs

# Автоматическая генерация при изменениях (в отдельном терминале)
flutter pub run build_runner watch

# Запуск приложения
flutter run

# Hot reload (во время работы приложения, в консоли)
r

# Hot restart (во время работы приложения, в консоли)
R

# Остановка приложения
q
```

## Режим разработки

По умолчанию приложение запускается в режиме разработки (`dev` flavor) с использованием моковых данных. Это означает:

- ✅ Не требуется подключение к реальному API
- ✅ Все функции работают с тестовыми данными
- ✅ Данные хранятся в памяти (сбрасываются при перезапуске)
- ✅ Можно тестировать весь функционал без бэкенда

## Дополнительная помощь

Если что-то не работает:

1. Проверьте раздел "Частые проблемы и решения" выше
2. Выполните `flutter doctor -v` и исправьте все проблемы
3. Прочитайте [README.md](./README.md) для подробной информации
4. Изучите логи приложения (они выводятся в консоль)

## Готово к демонстрации!

Приложение готово к демонстрации. Все функции работают с моковыми данными, поэтому можно показывать приложение без подключения к реальному API.

