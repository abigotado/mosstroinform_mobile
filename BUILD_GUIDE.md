# Руководство по сборке приложения

Подробные инструкции по сборке приложения для разных платформ.

## 📋 Содержание

- [Подготовка к сборке](#подготовка-к-сборке)
- [Android](#android)
- [iOS](#ios)
- [Веб (Web)](#веб-web)
- [Устранение проблем](#устранение-проблем)

## 🔧 Подготовка к сборке

### 1. Проверка окружения

Убедитесь, что все необходимое установлено:

```bash
flutter doctor -v
```

Все компоненты должны быть помечены как готовые (✓).

### 2. Очистка проекта

Перед сборкой рекомендуется очистить предыдущие сборки:

```bash
flutter clean
flutter pub get
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Проверка версии

Убедитесь, что вы используете правильную версию Flutter:

```bash
flutter --version
```

Требуется Flutter 3.10.0 или выше.

## 🤖 Android

### Требования

- Android Studio или Android SDK
- Android SDK Platform 33 или выше
- Java Development Kit (JDK) 21 или выше (требуется для Flutter 3.24.0)

### Debug сборка (для тестирования)

Debug сборка содержит отладочную информацию и не оптимизирована:

```bash
flutter build apk --debug
```

**Результат:** `build/app/outputs/flutter-apk/app-debug.apk`

**Установка на устройство:**
```bash
# Через ADB
adb install build/app/outputs/flutter-apk/app-debug.apk

# Или просто перетащите APK файл на устройство
```

### Release сборка (для распространения)

Release сборка оптимизирована и готова к распространению:

```bash
flutter build apk --release
```

**Результат:** `build/app/outputs/flutter-apk/app-release.apk`

**Особенности:**
- Код оптимизирован и минифицирован
- Отладочная информация удалена
- Размер файла меньше, чем debug версия

### App Bundle для Google Play

App Bundle (AAB) - рекомендуемый формат для публикации в Google Play:

```bash
flutter build appbundle --release
```

**Результат:** `build/app/outputs/bundle/release/app-release.aab`

**Загрузка в Google Play:**
1. Войдите в Google Play Console
2. Создайте новое приложение или выберите существующее
3. Перейдите в раздел "Release" → "Production" (или "Testing")
4. Загрузите файл `app-release.aab`

### Сборка для конкретной архитектуры

Для уменьшения размера APK можно собрать версию для конкретной архитектуры:

```bash
# Только для ARM64 (современные устройства)
flutter build apk --release --target-platform android-arm64

# Только для ARM32 (старые устройства)
flutter build apk --release --target-platform android-arm

# Только для x86_64 (эмуляторы)
flutter build apk --release --target-platform android-x64
```

### Split APK (разделение по архитектурам)

Создание отдельных APK для каждой архитектуры:

```bash
flutter build apk --release --split-per-abi
```

**Результат:**
- `app-armeabi-v7a-release.apk` (32-bit ARM)
- `app-arm64-v8a-release.apk` (64-bit ARM)
- `app-x86_64-release.apk` (x86_64)

### Подписание приложения

Для публикации в Google Play необходимо подписать приложение.

#### 1. Создание ключа подписи

```bash
keytool -genkey -v -keystore ~/upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```

#### 2. Создание файла key.properties

Создайте файл `android/key.properties`:

```properties
storePassword=<пароль_от_ключа>
keyPassword=<пароль_от_ключа>
keyAlias=upload
storeFile=<путь_к_keystore_файлу>
```

#### 3. Настройка build.gradle

Файл `android/app/build.gradle.kts` уже настроен для автоматической подписи при наличии `key.properties`.

#### 4. Сборка подписанного APK/AAB

```bash
flutter build apk --release
# или
flutter build appbundle --release
```

## 🍎 iOS

### Требования

- **macOS** (обязательно!)
- Xcode 14.0 или выше
- CocoaPods (устанавливается автоматически)
- Apple Developer Account (для установки на устройство и публикации)

### Настройка проекта

#### 1. Установка CocoaPods зависимостей

```bash
cd ios
pod install
cd ..
```

#### 2. Настройка подписи в Xcode

1. Откройте `ios/Runner.xcworkspace` в Xcode
2. Выберите проект "Runner" в навигаторе
3. Перейдите в раздел "Signing & Capabilities"
4. Выберите вашу команду разработчика (Team)
5. Xcode автоматически создаст provisioning profile

### Debug сборка

```bash
flutter build ios --debug
```

**Установка на симулятор:**
```bash
flutter run
```

**Установка на устройство:**
1. Подключите iPhone/iPad через USB
2. Доверьтесь компьютеру на устройстве
3. Выполните `flutter run`

### Release сборка

```bash
flutter build ios --release
```

### Создание архива для App Store

1. Откройте `ios/Runner.xcworkspace` в Xcode
2. Выберите "Any iOS Device" в списке устройств
3. Product → Archive
4. Дождитесь завершения архивации
5. В окне Organizer нажмите "Distribute App"
6. Следуйте инструкциям для загрузки в App Store Connect

### Установка на устройство без App Store

1. Соберите release версию: `flutter build ios --release`
2. Откройте `ios/Runner.xcworkspace` в Xcode
3. Выберите ваше устройство
4. Product → Run (или нажмите ⌘R)

### Конфигурация Info.plist

Для работы с видеопотоками и сетью уже настроены необходимые разрешения в `ios/Runner/Info.plist`:
- `NSAppTransportSecurity` - разрешения для сетевых запросов
- Разрешения для работы с камерой (если потребуется)

## 🌐 Веб (Web)

### Требования

- Chrome или другой современный браузер
- Flutter Web поддержка (включена по умолчанию)

### Debug сборка

```bash
flutter build web --debug
```

**Результат:** `build/web/`

**Запуск локального сервера:**
```bash
cd build/web
python3 -m http.server 8000
# или
npx serve
```

Откройте браузер: `http://localhost:8000`

### Release сборка

```bash
flutter build web --release
```

**Результат:** `build/web/` (оптимизированная версия)

### Деплой на веб-сервер

1. Соберите release версию: `flutter build web --release`
2. Загрузите содержимое папки `build/web/` на ваш веб-сервер
3. Настройте сервер для поддержки SPA (Single Page Application)

**Пример для Nginx:**
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

## 🔍 Устранение проблем

### Проблемы с Android сборкой

#### Ошибка: "Gradle build failed"

1. Очистите проект:
   ```bash
   flutter clean
   cd android
   ./gradlew clean
   cd ..
   ```

2. Обновите зависимости:
   ```bash
   flutter pub get
   cd ios && pod install && cd ..
   ```

3. Проверьте версию Gradle в `android/gradle/wrapper/gradle-wrapper.properties`

#### Ошибка: "SDK location not found"

Создайте файл `android/local.properties`:
```properties
sdk.dir=/path/to/Android/sdk
```

#### Ошибка подписи

Убедитесь, что файл `android/key.properties` существует и содержит правильные данные.

### Проблемы с iOS сборкой

#### Ошибка: "CocoaPods not installed"

```bash
sudo gem install cocoapods
cd ios
pod install
cd ..
```

#### Ошибка: "No valid code signing certificates"

1. Откройте Xcode
2. Xcode → Settings → Accounts
3. Добавьте ваш Apple ID
4. Выберите команду разработчика

#### Ошибка: "Provisioning profile not found"

1. В Xcode выберите проект Runner
2. Signing & Capabilities → выберите Team
3. Xcode автоматически создаст provisioning profile

#### Ошибка: "Command PhaseScriptExecution failed"

```bash
cd ios
rm -rf Pods Podfile.lock
pod install
cd ..
flutter clean
flutter pub get
```

### Проблемы с размером APK

#### APK слишком большой

1. Используйте split APK:
   ```bash
   flutter build apk --release --split-per-abi
   ```

2. Проверьте размер ассетов в `assets/`

3. Используйте App Bundle вместо APK для Google Play

### Проблемы с производительностью

#### Приложение работает медленно

1. Убедитесь, что используете release сборку для тестирования производительности
2. Проверьте логи через `AppLogger` на наличие проблем
3. Используйте Flutter DevTools для профилирования

## 📊 Размеры сборок

### Android APK (примерные размеры)

- **Debug APK:** ~50-70 MB
- **Release APK (universal):** ~25-35 MB
- **Release APK (arm64):** ~15-20 MB
- **Release App Bundle:** ~15-20 MB

### iOS (примерные размеры)

- **Debug:** ~50-70 MB
- **Release:** ~20-30 MB
- **App Store (после обработки):** ~15-25 MB

## ✅ Чеклист перед публикацией

### Android (Google Play)

- [ ] Собрана release версия: `flutter build appbundle --release`
- [ ] Приложение подписано
- [ ] Протестирована на разных устройствах
- [ ] Проверены все функции
- [ ] Подготовлены скриншоты и описание
- [ ] Настроены версии в `pubspec.yaml`

### iOS (App Store)

- [ ] Собран архив через Xcode
- [ ] Настроена подпись и provisioning profile
- [ ] Протестировано на реальных устройствах
- [ ] Проверены все функции
- [ ] Подготовлены скриншоты для разных размеров экранов
- [ ] Настроены версии в `pubspec.yaml` и Xcode

## 📝 Версионирование

Версия приложения задается в `pubspec.yaml`:

```yaml
version: 1.0.0+1
```

Формат: `версия+build_number`

- **версия** (1.0.0) - видимая пользователям версия
- **build_number** (+1) - номер сборки для магазинов приложений

При каждой публикации увеличивайте build_number.

## 🔐 Безопасность

### Рекомендации

1. **Не коммитьте ключи подписи** в репозиторий
2. Используйте `.gitignore` для исключения:
   - `android/key.properties`
   - `android/*.jks`
   - `ios/Pods/`
   - `*.keystore`

3. Храните ключи подписи в безопасном месте
4. Используйте разные ключи для разных окружений (mock/prod)

## 📚 Дополнительные ресурсы

- [Flutter Build and Release](https://docs.flutter.dev/deployment)
- [Android App Signing](https://developer.android.com/studio/publish/app-signing)
- [iOS App Distribution](https://developer.apple.com/distribute/)

